name: deploy

on:
  push:
    branches:
    - main

jobs:
  s3ls:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    steps:
      - uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-region: ${{ secrets.HOGE_TOKEN }}
          role-to-assume: ${{ secrets.HOGE_TOKEN }}
      - run: aws s3 ls